#include "../Struct/struct.h"
#include <stdio.h>
#include <stdlib.h>
#include <mysql.h>
#include <string.h>

void saveData(User * user){ //Function to send data to the database

}

void saveConfiguration(){
    
}

//add a function to save the data of the user in the database
    void saveData(User * user){    //Function to send data to the database
        //add a function to save the data of the user in the database
        saveUserData(user);
        //add a function to save the configuration of the user in the database
        saveUserConfiguration(user);
        //add a function to save the data of the character in the database
        //add a function to save the configuration of the character in the database
        //add a function to save the data of the inventory in the database
        //add a function to save the configuration of the inventory in the database
        //add a function to save the data of the equipment in the database
    }
//add a function to save the configuration of the user in the database
//add a function to save the data of the character in the database
//add a function to save the configuration of the character in the database
//add a function to save the data of the inventory in the database
//add a function to save the configuration of the inventory in the database
//add a function to save the data of the equipment in the database

//add a function to save the data of the user in the database
    void saveUserData(User * user){    //Function to send data to the database
        //add a function to save the data of the user in the database
        saveUserNickname(user);
        saveUserPassword(user);
        saveUserNbCharacters(user);
        saveUserCharacters(user);
    }

    void saveUserNickname(User * user){
        //add a function to save the nickname of the user in the database
    }
    void saveUserPassword(User * user){
        //add a function to save the password of the user in the database
    }
    void saveUserNbCharacters(User * user){
        //add a function to save the number of characters of the user in the database
    }
    void saveUserCharacters(User * user){
        //add a function to save the characters of the user in the database
    }

    void saveUserConfiguration(User * user){
        //add a function to save the configuration of the user in the database

    }
